<article class="blog-item p-3 mb-4">
    <!-- Title -->
    <a href="<%- config.root %><%- item.path %>">
        <h2 class="blog-post-title">
            <%- item.title %>
        </h2>
        <% if(item.subtitle) { %>
        <h3>
            <%- item.subtitle %>
        </h3>
        <% } %>
    </a>
    <!-- Date and Author -->
    <p class="meta mt-4">
        Posted on  
        <%= item.date.format('MMMM Do YYYY') %>
        <% if(item.author) { %>
        by <%- item.author %>
        <% } %>
    </p>
</article>